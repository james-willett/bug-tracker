pipeline {
    agent any

    stages {
        stage('Unit tests') {
            steps {
                dir ('bugtracker-backend') {
                      go test -v ./...
                }
            }
        }
    }
}
