pipeline {
    agent any

    stages {
        stage('Unit tests') {
            agent {
                docker{
                    image 'golang:1.20'
                    reuseNode true
            }
            steps {
                dir ('bugtracker-backend') {
                      sh 'go test -v ./...'
                }
            }
        }
    }
}
