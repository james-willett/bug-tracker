pipeline {
    agent any

    stages {
        stage('Unit tests') {
            steps {
                dir ('bugtracker-backend') {
                      sh 'go test -v ./...'
                }
            }
        }
    }
}
